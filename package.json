{
  "name": "greylux-admin-dashboard",
  "version": "0.1.0",
  "private": true,
  "description": "Greylux terminal-density finance admin dashboard (static HTML/CSS/JS, no runtime build). Dev-only test harness.",
  "type": "module",
  "scripts": {
    "test": "vitest --run"
  },
  "devDependencies": {
    "fast-check": "^3.19.0",
    "jsdom": "^24.1.3",
    "vitest": "^1.6.0"
  }
}
